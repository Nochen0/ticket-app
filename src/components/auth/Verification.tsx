import type { Component } from "solid-js"

type Props = {
  email: string
}

const Verification: Component<Props> = (props) => {
  return <div>Verification email sent</div>
}

export default Verification
